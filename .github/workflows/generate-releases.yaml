name: Generate Release

on:
  workflow_call:
    inputs:
      release_branch:
        required: false
        type: string
        default: main

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write       # Required to create tags/releases
      pull-requests: read   # Required to read commit history

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4 # Updated to v4 (standard)
        with:
          fetch-depth: 0      # ⚠️ Critical: Must fetch full history to see tags

      # 🟢 REPLACEMENT: Robust Version Calculator
      # This calculates the next version (v1.0.0 -> v1.0.1) automatically
      - name: Calculate next version
        id: version
        uses: mathieudutour/github-tag-action@v6.2
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          default_bump: patch   # 👈 This ensures it increments to v1.0.1 if no 'feat' is found
          release_branches: main
          dry_run: true         # We just want the number, we let the release step do the tagging

      # 🟢 SIMPLIFICATION: You do NOT need a manual 'git tag/push' step.
      # The release action below creates the tag automatically.

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          # Use the calculated tag from the previous step (e.g., v1.0.1)
          tag_name: ${{ steps.version.outputs.new_tag }}
          name: "Release ${{ steps.version.outputs.new_tag }}"
          generate_release_notes: true